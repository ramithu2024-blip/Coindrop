name: coindrop
description: "CoinDrop - Offline cash envelope budgeting app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite_common_ffi: ^2.3.4
  sqlcipher_flutter_libs: ^0.6.4
  path_provider: ^2.1.5
  intl: ^0.19.0
  path: ^1.9.0
  fl_chart: ^0.69.2
  shared_preferences: ^2.3.5
  csv: ^6.0.0
  encrypt: ^5.0.3
  crypto: ^3.0.6
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
